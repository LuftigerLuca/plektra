import 'dart:ui';

abstract final class AppColors {
  // Background
  static const background = Color(0xFF09090B);
  static const foreground = Color(0xFFFAFAFA);

  // Card
  static const card = Color(0xFF131316);
  static const cardForeground = Color(0xFFFAFAFA);

  // Primary (Violet)
  static const primary = Color(0xFF8B5CF6);
  static const primaryHover = Color(0xFFA78BFA);
  static const primaryForeground = Color(0xFF14091F);
  static const primaryBorder = Color(0xFF4C3A72);

  // Secondary
  static const secondary = Color(0xFF18181B);
  static const secondaryForeground = Color(0xFFFAFAFA);

  // Muted
  static const muted = Color(0xFF18181B);
  static const mutedForeground = Color(0xFFA1A1AA);
  static const mutedForegroundDim = Color(0xFF71717A);

  // Label / Disabled
  static const disabled = Color(0xFF3F3F46);

  // Border
  static const border = Color(0xFF27272A);
  static const borderSubtle = Color(0xFF1F1F23);

  // Success
  static const success = Color(0xFF4ADE80);
  static const successBorder = Color(0xFF1F4D3A);

  // Destructive
  static const destructive = Color(0xFFEF4444);
  static const destructiveForeground = Color(0xFFFAFAFA);

  // Ring
  static const ring = Color(0xFF8B5CF6);

  // Selection
  static const selection = Color(0x288B5CF6);
}
